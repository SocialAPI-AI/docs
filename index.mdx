---
title: SocialAPI.AI
description: Unified social media API. Manage your inbox, publish content, and monitor engagement across Instagram, Facebook, Threads, Google Business, TikTok, and LinkedIn through a single REST API.
---

## What is SocialAPI.AI?

[SocialAPI.AI](https://social-api.ai) gives your application a single API to manage social media across platforms. Read and reply to comments, DMs, and reviews. Create and schedule posts. Upload media. Get engagement metrics. Instead of integrating 6+ platform APIs, you integrate once.

You don't need to set up developer apps with Meta, TikTok, Google, LinkedIn, or YouTube — SocialAPI's apps are already approved and we handle the OAuth. Twitter is the one exception ([why](/guides/platform-credentials#why-twitter-is-different)).

<CardGroup cols={3}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Make your first API call in 5 minutes.
  </Card>
  <Card title="API Reference" icon="terminal" href="/api-reference/introduction">
    Full reference for every endpoint.
  </Card>
  <Card title="Platform Support" icon="grid-2" href="/guides/platforms">
    Capabilities by platform.
  </Card>
  <Card title="AI Assistants" icon="wand-magic-sparkles" href="/integrations/overview">
    Connect to Claude, ChatGPT, Cursor, and more.
  </Card>
  <Card title="Webhooks" icon="bolt" href="/guides/webhooks">
    Get notified of new comments, messages, and events.
  </Card>
  <Card title="Plans & Limits" icon="gauge" href="/guides/rate-limits">
    Resource limits and billing.
  </Card>
</CardGroup>

## Core capabilities

### Unified inbox

Read and respond to every customer interaction from a single API. Comments, DMs, and reviews are organized under `/v1/inbox` with consistent response shapes across all platforms.

```bash
# List posts with comments
curl https://api.social-api.ai/v1/inbox/comments \
  -H "Authorization: Bearer $SOCAPI_KEY"

# Reply to a comment
curl -X POST https://api.social-api.ai/v1/inbox/comments/POST_ID \
  -H "Authorization: Bearer $SOCAPI_KEY" \
  -H "Content-Type: application/json" \
  -d '{"text": "Thanks for your feedback!"}'

# List DM conversations
curl https://api.social-api.ai/v1/inbox/conversations \
  -H "Authorization: Bearer $SOCAPI_KEY"

# List reviews
curl https://api.social-api.ai/v1/inbox/reviews \
  -H "Authorization: Bearer $SOCAPI_KEY"
```

### Publishing

Create, schedule, and manage posts across platforms. Publish to multiple accounts at once, track delivery status per platform, and monitor engagement metrics.

```bash
# Create and schedule a post to Instagram and Facebook
curl -X POST https://api.social-api.ai/v1/posts \
  -H "Authorization: Bearer $SOCAPI_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Hello from SocialAPI!",
    "targets": [
      {"account_id": "acc_instagram", "platform": "instagram"},
      {"account_id": "acc_facebook", "platform": "facebook"}
    ],
    "scheduled_at": "2026-04-01T12:00:00Z"
  }'

# Check post metrics
curl https://api.social-api.ai/v1/posts/POST_ID/metrics \
  -H "Authorization: Bearer $SOCAPI_KEY"
```

### Media library

Upload images and videos, then reuse them across posts. Presigned upload URLs keep your media pipeline fast and secure.

```bash
# Get a presigned upload URL
curl "https://api.social-api.ai/v1/media/upload-url?filename=photo.jpg&content_type=image/jpeg" \
  -H "Authorization: Bearer $SOCAPI_KEY"
```

### AI assistant integrations

SocialAPI.AI exposes a full MCP server with 42 tools, so AI assistants like Claude, ChatGPT, and Cursor can manage your social accounts directly. OAuth 2.1 with PKCE keeps everything secure.

[Set up AI integrations &rarr;](/integrations/overview)

## How it works

1. **Connect** a social account via OAuth
```bash
curl -X POST https://api.social-api.ai/v1/accounts/connect \
  -H "Authorization: Bearer $SOCAPI_KEY" \
  -H "Content-Type: application/json" \
  -d '{"platform": "instagram"}'
```

2. **Read** your inbox (comments, DMs, reviews)
```bash
curl https://api.social-api.ai/v1/inbox/comments \
  -H "Authorization: Bearer $SOCAPI_KEY"
```

3. **Respond** to any interaction
```bash
curl -X POST https://api.social-api.ai/v1/inbox/comments/POST_ID \
  -H "Authorization: Bearer $SOCAPI_KEY" \
  -H "Content-Type: application/json" \
  -d '{"text": "Thanks!"}'
```

4. **Publish** content across platforms
```bash
curl -X POST https://api.social-api.ai/v1/posts \
  -H "Authorization: Bearer $SOCAPI_KEY" \
  -H "Content-Type: application/json" \
  -d '{"text": "New post!", "targets": [...]}'
```

## Authentication

All requests require a Bearer token:

```bash
Authorization: Bearer sapi_key_...
```

See [Authentication](/guides/authentication) for details on API keys, JWTs, and OAuth tokens.

## Supported platforms

| Platform | Posts | Comments | DMs | Reviews | Mentions | Publishing |
|---|---|---|---|---|---|---|
| Instagram | ✅ | ✅ | ✅ | - | ✅ | ✅ |
| Facebook | ✅ | ✅ | ✅ | - | ✅ | ✅ |
| Threads | ✅ | ✅ | - | - | - | ✅ |
| Google | ✅ | ✅ | - | ✅ | - | ✅ |
| TikTok | ✅ | - | - | - | - | ✅ |
| LinkedIn | ✅ | ✅ | - | - | - | ✅ |
| YouTube | ✅ | ✅ | - | - | - | ✅ |
| X / Twitter | Soon | Soon | Soon | - | Soon | - |
| Trustpilot | - | - | - | Soon | - | - |
